// function Pc(brand, processor, ram, storage){
//         this.brand = brand;
//         this.processor = processor;
//         this.ram = ram;
//         this.storage = storage;
// }

// const pc1 = new Pc("Intel", "i3 10100F", 16, 1000);


// function Keyboard(brand, type, isWireless){
//         this.brand = brand;
//         this.type = type;
//         this.isWireless = isWireless;
// }

// const key = new Keyboard("Logitech", "Mechanical", true);


// constructor (კონსტრუქტორი) არის სპეციალური ფუნქცია, რომელიც გამოიყენება ობიექტების შესაქმნელად და დასაწყისში მათთვის საწყისი


const brands = ["Dell", "Intel i3",];
const processor = ["Intel i3", "Intel i7"];
const storage = [512, 64, 120];

for (let i = 0; i < brands.length; i++);
for (let i = 0; i < processor.length; i++);
for (let i = 0; i < storage.length; i++);